# testing-retrospective.md —— 测试复盘报告（阶段⑦ 偏差验证 · 实施后回填）
# 模板来源：renheng B2B 订单履约验证项目 docs/06-testing-retrospective.html（已实战验证）
#
# 用途：里程碑复盘时核对 Score Card 用例的落地质量。与 roundtrip-report.md 一起构成
# 「验收总表」：roundtrip 回答「设计对了吗」，复盘回答「测试真的验证了吗」。
# 诚实原则（不可妥协）：
#   1. 零静默 Skip / 零假绿 / 零空测试 —— 逐项确认
#   2. 覆盖率差距诚实交代：差距在哪、为什么、追平方向是什么
#   3. 风险登记 R1-Rn：每个执行中新暴露风险都要「发现|根因|修复|教训」四段
#   4. 测试即设计的验收：错误注入逼出的真实缺陷比覆盖率数字更有价值

# ── 执行概况 ─────────────────────────────────────────────────────────────────
execution_summary:
  score_card_landed: "<N>/<M>（逐层列出，缺项必须标注）"
  extra_tests: "<补充测试描述：错误注入/边界分支/覆盖率补强，不改变场景映射>"
  total_tests: "<约 N 个 func Test>"
  silent_skip: "<零静默 Skip / 零假绿 / 零空测试：✅ 是 + 例外说明>"

# ── 覆盖率实测 ───────────────────────────────────────────────────────────────
coverage_measured:
  # 包: { target: <目标%>, actual: <实测%>, gap_note: <差距说明> }
  # domain: { target: 100%, actual: 100.0%, gap_note: "" }
  # api:    { target: 98%, actual: 91.5%, gap_note: "防御分支：userOf 兜底、日志注入" }
  <PACKAGE>: { target: <N>%, actual: <N>%, gap_note: "<差距说明>" }

honest_declaration: |
  <覆盖率与目标的差距（语句数/性质），全部位于什么分支>
  <错误注入手段 ≠ 真实故障形态；覆盖率数字 ≠ 故障注入等价性>
  <正确性核心与防线均有专项背书>

# ── 风险登记与处置 ───────────────────────────────────────────────────────────
risks_design_time:
  # 设计期已登记风险及其处置结果
  # - risk: "企微合规不通过"
  #   disposition: "未阻塞：mock 通道全链验证，官方通道封装就绪（wrapper 隔离点）"
  # - risk: "<风险>"
  #   disposition: "<处置结果>"

risks_execution:
  # 执行中新暴露风险：发现|根因|修复|教训 四段
  # - id: R1
  #   found: "事务内查询 pool 导致同事务可见性错误与连接池耗尽"
  #   root_cause: "冲突分支走 pool 而非同 tx"
  #   fix: "改走同 tx"
  #   lesson: "事务内禁止查 pool（单连接纪律）"
  # - id: <R-N>
  #   found: "<发现>"
  #   root_cause: "<根因>"
  #   fix: "<修复>"
  #   lesson: "<教训>"

# ── 覆盖倒推影响面（测试失败 → 设计点）──────────────────────────────────────
# 与 score-card.yaml 的 impact_if_fails 列呼应，按影响梯队聚合。
impact_tiers:
  # 第一梯队·守恒: [L1-03, L1-04, L1-05, L3-01]
  # 第二梯队·幂等: [L1-02, L2-12, L2-13]
  # 第三梯队·授权: [L1-07, L1-08, L1-09, L1-10]
  # 第四梯队·事件链: [L1-12, L3-05, L3-06]
  # 第五梯队·系统: [L3-02, L3-03, L3-04]
  <梯队名>: [<case ids>]

# ── 经验教训与后续建议 ───────────────────────────────────────────────────────
lessons:
  - "<Score Card 先行、覆盖率是结果不是目标 —— 全部落地，CQ→测试映射零漂移>"
  - "<测试即设计的验收：错误注入逼出 N 个真实缺陷，比覆盖率数字更有价值>"
  - "<剩余风险集中在：真实运行环境、覆盖率与目标的差距、业务规则待确认项>"
  - "<诚实交代：若需追平覆盖率，方向是 X 而非更多端到端测试>"

# ── 最终结论 ─────────────────────────────────────────────────────────────────
final_conclusion: |
  ☑ <Score Card 落地情况>；<domain 覆盖率>；<零静默 Skip 等>；<CQ→测试映射零漂移>
  ☑ <核心防线均有专项测试背书>
  ⚠️ <剩余风险集中在：真实运行环境 / 覆盖率差距 / 业务规则待确认项>
