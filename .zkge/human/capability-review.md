# 能力问题确认单

> 本文档是由 `.zkge/` 机器规约生成的人类阅读视图。
> **不要直接把本文件当作决策来源**；确认结果必须写入 `.zkge/decisions/`。

## 阅读说明

负责人只需要判断：

- 这个用户场景是否真实存在？
- 系统是否确实需要回答这个问题？
- 是否有遗漏、范围错误或不该做的能力？

填写结果请使用：`CONFIRM`、`REJECT`、`DEFER`。

## 能力问题

<!-- GENERATED_CLAIMS_START -->
<!-- generated_at: 2026-09-19T08:05:06Z; source: claims/*.yaml -->
| 业务编号 | 用户入口 | 能力问题 | 风险 | 技术状态 | 负责人决定 | 决策记录 |
|---|---|---|---|---|---|---|
| `C-005` | 主界面状态区 + 设置页识别方式 | 本机识别「服务在跑」不等于「能识别」：模型文件缺失或加载失败时，服务仍在、探测照过，主界面照样说「就绪」。 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-042` | 主界面录音中本机识别服务退出 | 本机识别中途崩了，用户正在说的那几句是丢了还是保住了？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-045` | 主界面长时间快速说话 | 用户说得又快又多，识别跟不上的时候，中间那段话会不会被悄悄丢掉而用户完全不知道？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-048` | 主界面录音后看记录列表 | 被应用判定成噪声而丢掉的那些话，用户有没有任何办法找回来？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-040` | 设置页密钥输入框 | 密钥存在这台电脑的什么地方？别人拿到这台电脑能不能直接看到？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `C-001` | 主界面状态区 + 录音按钮 | 麦克风被系统或硬件关掉（键盘静音键、菜单栏静音、输入音量拉到 0）时，应用以为还在录，实际录进来的是空气。录完整场一个字没有，状态区全程写「识别中」。 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `C-002` | 录音中途识别服务掉线：界面继续写「录音中」，此后每一句都在悄悄丢 | S-021 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-009` | 主界面长时间连续说话 | 用户一口气不停说了半分钟以上，会不会被切成几段？切开的地方会不会把字吃掉？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-024` | 设置页清除历史 + 主界面录音中 | 用户一边说话一边点了「清除历史记录」，正在说的这句会不会被清掉，或者清完之后又自己冒回来？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-209` | 白板编辑模式 + 外部写入 | 外部 AI 助手写回来的时候，会不会把用户自己写的内容覆盖掉？用户事先知道吗？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-166` | 白板编辑模式 + 外部写入 | 外部 AI 助手正在写文档的时候用户也在编辑，会不会互相盖掉？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-162` | 白板只读模式 | 白板处于只读模式时用户改不了，但外部 AI 助手还在写，用户会不会莫名其妙看着内容变掉？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-170` | 白板会议列表 | 用户怎么删掉一场会？删了之后这场会的说话记录会不会一起没了？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-214` | 白板 + 外部 AI 助手连续写入 | 外部 AI 助手连着写两次，第一次写的内容是不是就被第二次盖掉了？用户知道会这样吗？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `C-024` | 数据目录里的白板文档 / 会议白板 | 白板正文同时存在两个「存放地」：程序用来展示的那份，和给人看的文档文件。用户改了后者（这是最自然的做法），白板毫无反应，改动像掉进黑洞。 | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-096` | 指令管理页保存 + 外部改文件 | 用户一边在管理页改，外部 AI 助手一边在文件里改，会不会有一方的改动被整个抹掉？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `C-017` | 语音指令页 | 外部 AI 删掉的一条口令，用户随后编辑并保存了同一块（他并不知道它被删了）：那条被悄悄「复活」。 | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-095` | 指令管理页「恢复默认」 | 用户点「恢复默认」，自己辛苦配的指令是不是立刻全没了？有没有二次确认？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `C-036` | 设置页底部 | 用户点了「恢复默认」再点「取消」——改动依然生效，因为「取消」只是把这一页关掉。用户以为「取消」能兜住，实际兜不住。 | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-140` | 设置页保存 + 外部改配置 | 用户在界面上改设置、外部 AI 助手同时在改同一个配置，谁会被覆盖？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-143` | 手工编辑配置文件后打开应用 | 配置文件被改坏或者损坏时，应用还能不能启动？会不会直接把用户所有设置重置掉？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-128` | 设置页「恢复默认」按钮 | 用户点「恢复默认」，到底哪些东西会变回去？界面有没有把范围讲清楚？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-147` | 版本回退后打开设置页 | 以后配置里加出新项目，老版本的应用会不会把新项目抹掉？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `C-037` | 任何界面（双开时） | 同一份用户数据被两个应用实例同时使用（开发模式跑着一个、装好的应用也开着）。两边录音都往同一份记录里写、设置互相覆盖，记录混在一起。 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-210` | 外部 AI 助手接入 / 浏览器打开的网页 | 是不是这台电脑上任何程序都能读写这些东西？别的设备或者一个网页能不能？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-218` | 应用运行中 + 本机其它程序 | 允许外部接入之后，这台电脑上别的软件有没有机会顺手读走用户的说话记录和账号密钥？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-189` | 合盖唤醒后看主界面状态区 | 用户合上笔记本盖子，过一会儿打开，界面看起来一切正常吗？还是有明确的异常提示？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-190` | 唤醒后主界面继续录音 | 唤醒之后用户接着说话，这些话能不能真的被记下来？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-105` | 主界面底部自动发送开关 | 用户打开自动发送后，粘完是不是直接按了回车把消息发出去了？这条消息还能撤回吗？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-230` | 应用更新后使用本机识别 | 应用更新时，它依赖的本机识别组件会不会一起更新？不一起更新会不会出现功能坏掉？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `C-003` | 主界面列表 + 录音中 | 那句话明明显示在屏幕上了，却没写进记录文件（磁盘满、数据目录被移走、目录不可写）。用户当场看得见，重开应用这句话就没了。 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-064` | 主界面记录列表 | 一条记录只写了一半就被打断（比如应用被强退），用户看到的是缺字的那条还是干脆没有？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-072` | 设置页清除历史记录按钮 | 用户点「清除历史记录」，是不是真的删干净了？还能不能找回？他清之前知不知道后果？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `C-034` | 设置页清除历史 → 会议白板 | 「清除历史」会把通往已保存白板正文的路一起铲掉：白板的会议场次是从记录文件反推出来的，记录没了，场次列表塌掉，用户之前存过的 AI 正文还躺在电脑上，界面上却再也没有入口找到它们。 | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-078` | 设置页清除历史记录之后 | 清除之后，别的地方（日志、缓存、备份）还会不会留着副本？ | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
| `S-225` | 录音中 + 更新提示出现并安装 | 用户正在录音时弹出更新提示，如果这时装了，正在录的内容会不会丢？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `S-228` | 更新后打开应用各页面 | 更新会不会动到用户的记录、设置、指令表或者白板内容？ | R3 | `UNKNOWN` / A3 | `待确认` | `.zkge/decisions/` |
| `C-043` | 底栏自动粘贴开关 / 更新后 | 系统对辅助功能的授权是跟着应用身份走的：应用更新之后这项授权可能失效，用户开着自动粘贴却不出字，而且不会收到任何新提示。 | R3 | `UNKNOWN` / A2 | `待确认` | `.zkge/decisions/` |
<!-- GENERATED_CLAIMS_END -->

## 维度覆盖

| 维度 | 是否有问题 | 负责人补充 |
|---|---|---|
| 正常路径 | 待生成 | |
| 边界输入 | 待生成 | |
| 权限与越权 | 待生成 | |
| 前置状态 | 待生成 | |
| 并发与一致性 | 待生成 | |
| 数据生命周期 | 待生成 | |
| 集成与扩展 | 待生成 | |
| 安全与隐私 | 待生成 | |

## 需要确认的非能力事项

<!-- GENERATED_DECISIONS_START -->
| 决策编号 | 对象 | 为什么需要人决定 | 默认处理 | 记录方式 |
|---|---|---|---|---|
| `DECISION.SCOPE.001` | 能力问题是否属于产品范围 | 机器不能判断业务是否真实需要 | `DEFER` | 创建 `.zkge/decisions/DECISION.SCOPE.001.yaml` |
| `DECISION.ORACLE.001` | 技术 oracle 是否足以代表业务要求 | 技术负责人确认验证方式是否正确 | `DEFER` | 创建对应 decision 文件 |
| `DECISION.EXCEPTION.001` | 是否接受已知风险或跳过 SHOULD | 例外必须有 owner 和到期时间 | `REJECT` | 使用 `category: exception` |
<!-- GENERATED_DECISIONS_END -->

## 确认记录

- 确认人：
- 角色：
- 日期：
- 决策文件：`.zkge/decisions/`
